"use strict";
function test(n1, n2, mostrar, frase) {
    var resp = n1 + n2;
    if (mostrar) {
        console.log(frase + resp);
    }
    else {
        return resp;
    }
}
var lista, number = ['Maura',];
